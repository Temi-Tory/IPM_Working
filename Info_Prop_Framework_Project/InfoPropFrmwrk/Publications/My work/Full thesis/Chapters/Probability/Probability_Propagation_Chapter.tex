\ifdefined\maindoc
% Do nothing (we are inside main.tex)
\else
\documentclass[12pt]{report}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{enumitem}
\usepackage{placeins}
\usepackage{hyperref}
\usepackage{array}
\usepackage{makecell}
\usepackage[backend=biber,style=ieee,citestyle=numeric-comp,sorting=none]{biblatex}
\addbibresource{../../references.bib}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{proposition}[theorem]{Proposition}
\onehalfspacing
\graphicspath{{figures/}}
% Standalone-only fallbacks so Chapter~\ref{...} renders as the right number outside the
% main thesis. Under \maindoc these are skipped and the real labels in main.tex resolve.
\makeatletter
\newlabel{ch:system-model}{{3}{1}{}{Doc-Start}{}}
\newlabel{ch:diamond-module}{{5}{1}{}{Doc-Start}{}}
\newlabel{ch:lit-review}{{2}{1}{}{Doc-Start}{}}
\newlabel{ch:capacity-toolkit}{{6}{1}{}{Doc-Start}{}}
\newlabel{ch:critical-path}{{7}{1}{}{Doc-Start}{}}
\newlabel{ch:julia-package}{{8}{1}{}{Doc-Start}{}}
\newlabel{ch:front-end}{{9}{1}{}{Doc-Start}{}}
\newlabel{ch:conclusions}{{11}{1}{}{Doc-Start}{}}
\newlabel{app:network-specs}{{A}{1}{}{Doc-Start}{}}
\newlabel{app:full-results}{{B}{1}{}{Doc-Start}{}}
\makeatother
\begin{document}
\title{Information as Probabilities: Probability Propagation Toolkit}
\fi

\providecommand{\Pa}{\mathrm{Pa}}
\providecommand{\anc}{\mathrm{anc}}
\providecommand{\infl}{\mathrm{infl}}



\section{Introduction}
\label{sec:pp-intro}

Under the probabilistic interpretation of the network model, the value on a node is the
probability that the component operates and the value on an edge the probability that the
handover between its endpoints succeeds. The question this interpretation makes precise is
with what probability each node is reached from the sources despite component and handover
failures. The Probability Propagation Toolkit answers that question exactly, for every node
of the network at once. Its algorithm, the Probability Propagation Algorithm (PPA), was first
published as the Information Propagation Method \cite{ohiani_information_2023}; the name
used in this thesis distinguishes the algorithm from the framework that carries it.

The toolkit reads the graph object of Chapter~\ref{ch:system-model} and, wherever the
network reconverges, the decomposition of Chapter~\ref{ch:diamond-module}. It is the analysis
the decomposition module was built for, because reconvergent paths are where local
probability calculations stop being valid. A local update at a join treats its parents as
independent, and when the parents share an upstream component that treatment overstates the
join's belief. The algorithm repairs this by conditioning on the shared fork, computing each
branch separately and recombining by total probability, and it applies the same recursion to
nested and overlapping diamonds through the supernode reduction of
Section~\ref{sec:pp-supernodes}.

The chapter makes four claims and supports each. The propagation is exact for point-valued
inputs, proved in Sections~\ref{sec:pp-conditioning} and~\ref{sec:pp-supernodes} and checked
against an independent decision-diagram oracle on a corpus of 129 graphs, on named and
adversarial families, and on two published benchmarks whose exact values are reproduced
(Section~\ref{sec:pp-validation}). Its cost is governed by the same structural width that
governs a well-ordered decision diagram, and the two methods are competitive for exact
point-valued reliability with neither dominating (Section~\ref{sec:pp-complexity}). For
interval inputs the propagation returns the exact range at machine precision, at lower
one-shot cost than the decision-diagram route (Section~\ref{sec:pp-interval}). For
probability-box inputs it returns sound bounds on the distribution of every belief, whose
tightness is characterised by an envelope over network structure and input uncertainty
(Section~\ref{sec:pp-pbox}). Section~\ref{sec:pp-case} applies the toolkit to a medical drone
delivery network, Section~\ref{sec:pp-discussion} states the limits of the method, and
Section~\ref{sec:pp-summary} concludes. Before the method, Section~\ref{sec:pp-belief}
defines the quantity computed and Section~\ref{sec:pp-pass} the single pass on which
everything is built.

Chapter~\ref{ch:lit-review} reviewed the exact reliability methods against which this
toolkit is placed. Three points from that review are used here. Every exact method is
exponential in some width of the network, because the problem is \#P-hard, and the
question is which width and at what constant. The binary decision diagram, with a variable
ordering found by sifting, is the standard this chapter compares against, since an unordered
diagram is not a fair baseline for any method. And every exact method reviewed consumes a
point-valued component probability; the interval and probability-box forms of
Chapter~\ref{ch:lit-review} are what this toolkit propagates that the others do not.

\section{The Belief of a Node}
\label{sec:pp-belief}

Every component of the network can fail. A node $v$ carries its prior $\pi_v$, the probability that the component operates, and an edge $(u,v)$ carries its probability $\rho_{uv}$, the probability that the handover from $u$ to $v$ succeeds, both attached to the graph object during input processing. Writing $X_v$ and $Y_{uv}$ for the corresponding indicator variables, the independence assumption of Chapter~\ref{ch:system-model} makes all of them mutually independent.

Information reaches a node when some path delivers it, and whether it does is itself an indicator variable. The reachability of $v$ is
\begin{equation}
\label{eq:pp-indicator}
R_v \;=\; X_v \,\wedge\, \Bigl(\, v \in S \;\vee\; \bigvee_{u \in \Pa(v)} \bigl( R_u \wedge Y_{uv} \bigr) \Bigr),
\end{equation}
where $S$ is the source set and $\Pa(v)$ the parents of $v$. The toolkit computes the \emph{belief} of every node,
\begin{equation}
\label{eq:pp-belief}
b(v) \;=\; P(R_v = 1),
\end{equation}
the probability that $v$ operates and is reached from at least one source. Unrolling the recursion writes $R_v$ as a disjunction over all source-to-$v$ paths of the conjunction of each path's node and edge indicators, so the belief is equally the probability that at least one path is viable end to end.

Computing $b(v)$ subsumes two-terminal network reliability and is therefore \#P-hard \cite{valiant1979complexity,ball1986computational}. No exact method escapes exponential cost on every network, and the aim is exactness at a cost set by the structure of the particular network. $R_v$ is also a monotone Boolean function of the component indicators, since restoring a failed component can only create source-to-node connectivity, never destroy it. Both facts are used throughout the chapter.

\begin{proposition}[Monotonicity]
\label{prop:pp-monotone}
$b(v)$ is non-decreasing in every node prior and every edge probability.
\end{proposition}

\begin{proof}
Realise each indicator from its own independent uniform variable, $X_w = \mathbf{1}\{U_w \le \pi_w\}$ and $Y_{uw} = \mathbf{1}\{U_{uw} \le \rho_{uw}\}$. Raising any single $\pi_w$ or $\rho_{uw}$ can switch its indicator from $0$ to $1$ but never from $1$ to $0$, and $R_v$ is monotone in every indicator, so on every realisation $R_v$ can only switch from $0$ to $1$. Hence $P(R_v = 1)$ cannot decrease.
\end{proof}

\section{Propagation in a Single Pass}
\label{sec:pp-pass}

A source is reached exactly when it operates, so its belief is its prior. Every other node receives information through its parents. The \emph{signal} through a parent $p$ of $v$ is the event $A_p$ that a viable path reaches $p$ and the handover to $v$ succeeds, with probability
\begin{equation}
\label{eq:pp-signal}
S_p \;=\; b(p)\,\rho_{pv},
\end{equation}
and $v$ is reached when at least one signal arrives, so
\begin{equation}
\label{eq:pp-union}
b(v) \;=\; \pi_v \, P\Bigl(\bigcup_{p \in \Pa(v)} A_p\Bigr).
\end{equation}
When the signal events are mutually independent the union has the closed form
\begin{equation}
\label{eq:pp-independent}
b(v) \;=\; \pi_v \Bigl( 1 - \prod_{p \in \Pa(v)} \bigl( 1 - b(p)\,\rho_{pv} \bigr) \Bigr),
\end{equation}
which for a single parent collapses to the chain rule $b(v) = \pi_v\, b(p)\, \rho_{pv}$.

The iteration sets of Chapter~\ref{ch:system-model} order the computation so that every parent's belief exists before it is consumed. The algorithm initialises the sources, then processes each layer in turn, applying the local update once per node, and the layered order of Chapter~\ref{ch:system-model} makes the single forward pass complete in exactly $|L|$ layer steps. There is no iterative refinement to converge and no sampling to average, because every update is an exact probability calculation.

Equation~\eqref{eq:pp-independent} evaluates the union as a product of complements instead of by the alternating inclusion-exclusion sum. Under independence the two are identical, but in the product form each signal probability occurs exactly once, which keeps the evaluation numerically stable and, once values become intervals, avoids the artificial widening that repeated occurrences of a variable inflict on interval arithmetic (Section~\ref{sec:pp-interval}).

The update rests on one hypothesis. The update of Eq.~\eqref{eq:pp-independent} is exact when the signal events are independent, and they are independent when the paths feeding the parents share no component upstream. In a tree that is always true. In the systems this framework serves, however, it is false, because Chapter~\ref{ch:system-model} established that engineered redundancy is reconvergence, with duplicated routes sharing the components upstream of the fork that created them.

\section{Reconvergence Breaks the Local Update}
\label{sec:pp-failure}

Figure~\ref{fig:pp-simple} shows the smallest network on which the local update fails, a diamond in the sense of Chapter~\ref{ch:diamond-module}. Source $1$ has prior $1$, every other node has prior $0.9$, and every edge has probability $0.9$. Every worked number in this chapter is the output of the implementation on the stated inputs, checked in the same run against an independent exact computation, exhaustive enumeration of all component states here and exhaustive path enumeration with inclusion-exclusion for the larger example of Section~\ref{sec:pp-worked}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.3\linewidth]{fig01_simple_diamond/fig01_simple_diamond.pdf}
  \caption{The simple diamond. Source 1 feeds fork 2 (bold), whose two paths reconverge at join 5 (shaded).}
  \label{fig:pp-simple}
\end{figure}

The forward pass runs cleanly until the join. The fork's belief is the chain $b(2) = 1 \times 0.9 \times 0.9 = 0.81$, and each intermediate node follows as $b(3) = b(4) = 0.81 \times 0.9 \times 0.9 = 0.6561$, so each signal into the join carries $S = 0.6561 \times 0.9 = 0.59049$. Treating the two signals as independent, Eq.~\eqref{eq:pp-independent} gives
\[
b(5) \;=\; 0.9\,\bigl(1 - (1 - 0.59049)^2\bigr) \;=\; 0.749071 ,
\]
while exhaustive enumeration over all component states gives $b(5) = 0.675462$. The local update overestimates the join's belief by eleven per cent of its true value, and it does so because both signal events contain the fork's reachability. The two events are positively dependent, their intersection is larger than the product of their probabilities, and a union rule that understates the intersection overstates the union. On a network built out of redundancy the error is the norm, and it always overstates the reliability of the system.

However, conditioning on the shared fork repairs the calculation. Given $R_2 = 1$ the two paths share nothing further, so each transmits independently with probability $0.9 \times 0.9 \times 0.9 = 0.729$ and the join receives a signal with probability $1 - (1 - 0.729)^2 = 0.926559$. Given $R_2 = 0$ nothing arrives, since every path into the join runs through the fork. Weighting the two cases by $P(R_2 = 1) = b(2) = 0.81$,
\[
P(\text{signal at } 5) \;=\; 0.81 \times 0.926559 \;=\; 0.750513 ,
\qquad
b(5) \;=\; 0.9 \times 0.750513 \;=\; 0.675462 ,
\]
the exact value. Two conditioned sub-problems replace an enumeration over $2^{10}$ component states, because conditioning is applied only to the one variable whose sharing created the dependence. On real networks, where diamonds nest, overlap and arrive several to a join, the same economy requires the machinery of Chapter~\ref{ch:diamond-module}.

\section{Conditioning at a Join}
\label{sec:pp-conditioning}

The example used two facts without naming them, that a belief can be split over the states of other nodes and that fixing the right node makes the parents' signals independent. A third governs the joins the example was too small to show, where several groups of parents arrive sharing nothing between them and each group's contribution must recombine with the others. All three hold in general, and together they justify the algorithm's treatment of any join. The structural objects come from Chapter~\ref{ch:diamond-module}, whose decomposition partitions each join's parents into groups of shared unfixed ancestry, anchors each group's diamond, and sets aside the non-influencing parents. A diamond arrives carrying its set $C$ of fixed nodes, and since the propagation resolves the diamond by conditioning on the reachability states of exactly those nodes, this chapter calls $C$ the \emph{conditioning set}.

\begin{lemma}[Conditional invariance]
\label{lem:pp-invariance}
For any set $A \subseteq V$ and any node $v$,
\[
b(v) \;=\; \sum_{c} P(R_A = c)\; P(R_v = 1 \mid R_A = c),
\]
where $R_A$ is the vector of reachability indicators of $A$ and the sum runs over its states $c \in \{0,1\}^{|A|}$. Every term is well defined because each $R_u$ is a deterministic function of the mutually independent component indicators.
\end{lemma}

\begin{proof}
The events $\{R_A = c\}$ partition the sample space, and the display is the law of total probability over that partition.
\end{proof}

\begin{lemma}[Separator sufficiency]
\label{lem:pp-separator}
Let $v$ be a join evaluated within a network in which every node of a fixed set $C$ enters only as a local source whose state is pinned, and suppose the influencing sets $\infl(u, C) = \anc(u) \setminus C$ of $v$'s parents within that network are pairwise disjoint. Then, conditional on $R_C = c$, the parents' signal events are mutually independent, and
\[
b(v \mid R_C = c) \;=\; \pi_v \Bigl( 1 - \prod_{u \in \Pa(v)} \bigl( 1 - P(R_u = 1 \mid R_C = c)\,\rho_{uv} \bigr) \Bigr).
\]
\end{lemma}

\begin{proof}
Every path into a parent $u$ either starts at a node of $C$, whose pinned state replaces everything upstream of it, or runs through components of $\infl(u, C)$ alone. Conditional on $R_C = c$, the signal event through $u$ is therefore a function of the constant $c$, of the indicators of the nodes of $\infl(u, C)$, of the edges among them, and of the edge $(u,v)$. The influencing sets are pairwise disjoint, so these indicator sets are pairwise disjoint and mutually independent, and functions of disjoint sets of independent variables are independent. The display is Eq.~\eqref{eq:pp-independent} applied conditionally.
\end{proof}

Chapter~\ref{ch:diamond-module} establishes both hypotheses at every site where the propagation applies the independent update. Its edge isolation lemma guarantees that a fixed node appears in a diamond's edge list only as a local source, with its outgoing edges kept and its upstream structure cut. Its grouping theorem certifies disjoint influencing sets, and the recursion extends the conditioning one covering fork at a time until the parents in view are disjoint in this sense. The conditional beliefs the display consumes are the \emph{contextual beliefs}, the values each node holds within the sub-network currently being evaluated, which are conditional on the ambient states by construction because they were computed there.

The remaining question is the one Chapter~\ref{ch:diamond-module} deferred, whether the contributions of separately decomposed groups recombine correctly at the join. Its group disjointness theorem gives distinct groups pairwise disjoint influencing sets under the current context, and disjoint influence is exactly independence.

\begin{lemma}[Recombination across groups]
\label{lem:pp-recombination}
Fix a join $v$ and a context $E$ whose nodes' states are given, and let $G_1, \dots, G_m$ be the parent groups of the decomposition's partition at $v$, together with the singleton groups forming the non-influencing set. Write $E_k$ for the event that some parent in $G_k$ delivers a signal to $v$. The events $E_1, \dots, E_m$ are mutually independent, so
\[
P(\text{signal at } v) \;=\; 1 - \prod_{k=1}^{m} \bigl( 1 - P(E_k) \bigr),
\]
and each group is resolved on its own conditioning set alone.
\end{lemma}

\begin{proof}
Each $E_k$ is a function of the indicators of the nodes in $\bigcup_{p \in G_k} \infl(p, E)$, of the edges among them, and of the edges from $G_k$ into $v$. By group disjointness the node sets are pairwise disjoint, hence so are the edge sets they induce, and the given context enters every $E_k$ only as fixed constants. The events are therefore functions of pairwise disjoint sets of mutually independent indicators, hence mutually independent, and the display is the union rule for independent events.
\end{proof}

The lemma also covers the non-influencing set of Chapter~\ref{ch:diamond-module}. A non-influencing parent is a singleton group, its influencing set meeting no other's, so its signal enters the product as one more independent factor at the cost of a single multiplication, with no conditioning at all. A join can therefore gather several diamonds and several plain parents, and the recombination stays a one-line product over independent contributions.

Recombination across groups is where the decomposition reduces the cost. A join whose $m$ groups carry conditioning sets $C_1, \dots, C_m$ is resolved in $\sum_k 2^{|C_k|}$ conditioned sub-problems, where conditioning on all the groups jointly would take $2^{\sum_k |C_k|}$. The gap is widest at high fan-in over independent structure. On an adversarial family in which $k$ independent reconvergent branches meet at one join, the implementation resolves exactly $2k + 1$ conditioned sub-problems for every $k$ tested up to $16$, linear where joint conditioning would be exponential.

What remains at a single group is the enumeration itself. Writing $C = \{a_1, \dots, a_m\}$ for the group's conditioning set, total probability gives
\begin{equation}
\label{eq:pp-enumeration}
P(E_k) \;=\; \sum_{c \,\in\, \{0,1\}^{m}} \; \Bigl( \prod_{i=1}^{m} \, b(a_i)^{c_i} \bigl(1 - b(a_i)\bigr)^{1 - c_i} \Bigr) \; \psi(c),
\end{equation}
where the weights are built from the conditioning nodes' contextual beliefs and $\psi(c)$ is the probability that the group delivers a signal to $v$ given the state $c$. A conditioning state does not simplify the diamond to a formula. It poses a smaller instance of the whole problem, which the algorithm solves in the same way.

\section{Supernodes and Progressive Decomposition}
\label{sec:pp-supernodes}

A diamond arrives from Chapter~\ref{ch:diamond-module} as a graph object of the same form as the network itself, and the propagation exploits that self-similarity directly. For each state $c$ of the conditioning set, the algorithm poses the diamond as a network in its own right and runs on it the same single pass it runs on the whole. The local sources of that sub-network take their values from the enclosing computation, a conditioning node pinned to its state and every other local source keeping its contextual belief. The join's own prior is deferred, set to $1$ inside the sub-problem so that the sub-run returns the probability that a signal arrives, with the join's prior applied exactly once by the enclosing update. The value the sub-run returns at the join is $\psi(c)$.

The map $c \mapsto \psi(c)$ is all the enclosing computation needs from the diamond. The solved diamond is therefore replaced by a \emph{supernode}, a single equivalent component that delivers a signal to the join with probability $\psi(c)$ under each state of its conditioning set.

\begin{lemma}[Supernode equivalence]
\label{lem:pp-supernode}
Let $D$ be a diamond at join $j$ with conditioning set $C$, solved in its context to the map $\psi_D$. Replacing $D$ by a supernode that delivers a signal to $j$ with probability $\psi_D(c)$ under each state $c$ leaves the belief of every node downstream of $j$ unchanged.
\end{lemma}

\begin{proof}
Fix a state $c$. Conditional on $R_C = c$, the event that $D$ delivers a signal to $j$ is a function of the indicators internal to $D$, which are independent of every indicator outside $D$. The enclosing computation only ever conditions on the nodes of its own conditioning sets, each of which enters the sub-network containing $D$ as a local source, upstream of $D$ or beside it, so each such conditioning event is a function of indicators outside $D$ and leaves $P(\text{signal at } j \mid R_C = c) = \psi_D(c)$ unchanged. A downstream belief depends on $D$ only through the signal event at $j$, entering by Lemma~\ref{lem:pp-invariance} as the pair of the weights $P(R_C = c)$ and the conditionals $\psi_D(c)$, and the supernode preserves both.
\end{proof}

The proof's independence step needs the enclosing computation to fix no node internal to $D$, and the decomposition guarantees this. However, when an outer conditioning does reach inside a subgraph, the context-aware identity of Chapter~\ref{ch:diamond-module} makes the inner structure a different diamond with its own stored entry, so the sub-problem solved is always the one whose context holds.

Nesting makes the reduction progressive. Within a state's sub-run, an inner join with unresolved shared ancestry carries its own diamond, recorded by the decomposition in the enclosing diamond's substructure table, and the sub-run resolves it by the same conditioning, from the inside out. Termination is inherited from Chapter~\ref{ch:diamond-module}, whose recursion bounds the nesting by strictly growing the fixed context, and exactness follows by induction on nesting depth, each level applying Lemmas~\ref{lem:pp-invariance} to~\ref{lem:pp-supernode} within its own sub-network, which satisfies the same model assumptions as the whole.

Two mechanisms keep the recursion from repeating itself. A conditioned sub-problem is identified by its diamond's edge list together with the local source values its state assigns, and every solved sub-problem is stored under that identity, so a structurally identical sub-problem posed anywhere else in the recursion is answered from the store. Separately, a conditioning node whose contextual belief is already pinned to $0$ or $1$ by the enclosing state carries no remaining uncertainty, so its enumeration collapses to the single state of non-zero weight. The collapse is exact because the discarded state has weight exactly $b\,{=}\,0$ or $1 - b\,{=}\,0$ in Eq.~\eqref{eq:pp-enumeration}.

\subsection{A multi-level worked example}
\label{sec:pp-worked}

The network of Figure~\ref{fig:pp-worked} contains every structure of the preceding sections: three sources, three forks, nested diamonds, a join fed by a diamond and a non-influencing parent together, and a target whose reconvergence encloses every other structure. Sources $S_1$, $S_2$, $S_3$ have prior $1$, every other node prior $0.9$, and every edge probability $0.9$.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.5\linewidth]{fig03_worked_network/fig03_worked_network.pdf}
  \caption{The multi-level worked network. Sources are double-bordered, forks 1, 7 and 14 bold, joins 4, 6, 11, 12 and the target $T$ shaded.}
  \label{fig:pp-worked}
\end{figure}

The decomposition of Chapter~\ref{ch:diamond-module} returns four maximal diamonds. $D_1$ at join 6 anchors on fork 1, covering the reconvergent parents 5 and 3, while parent 13 arrives from $S_2$'s side sharing nothing with them and lands in the join's non-influencing set. $D_2$ at join 11 anchors on fork 7 and $D_3$ at join 12 on fork 14. $D_4$ at the target anchors on fork 1, because the target's two parents, 12 and 11, share only node 1 in their unfixed ancestry, and within $D_4$'s span the reconvergences at joins 11 and 12 recur as sub-diamonds. Join 4 receives parents 1 and 8 whose ancestries are disjoint, so it needs no diamond anywhere, and the plain update of Eq.~\eqref{eq:pp-independent} handles it at every level.

Table~\ref{tab:pp-trace} traces the four resolutions in the terms of Sections~\ref{sec:pp-conditioning} and~\ref{sec:pp-supernodes}. Each is a two-state enumeration whose weight is the conditioning node's contextual belief, here $0.81$ in all four cases since forks 1, 7 and 14 each sit one perfect source, one edge and one prior away.

\begin{table}[htbp]
\centering
\caption{Conditioning trace of the worked network. $w$ is the conditioning node's contextual belief, $\psi(1)$ and $\psi(0)$ the conditional signal probabilities the diamond's sub-runs return at the join, and $P(E)$ the group's recombined signal $w\,\psi(1) + (1-w)\,\psi(0)$.}
\label{tab:pp-trace}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lcccccc}
\toprule
Diamond & Join & $C$ & $w$ & $\psi(1)$ & $\psi(0)$ & $P(E)$ \\
\midrule
$D_1$ & 6  & $\{1\}$  & 0.81 & 0.889023 & 0        & 0.720108 \\
$D_2$ & 11 & $\{7\}$  & 0.81 & 0.893745 & 0.478297 & 0.814810 \\
$D_3$ & 12 & $\{14\}$ & 0.81 & 0.931860 & 0.583288 & 0.865631 \\
$D_4$ & $T$ & $\{1\}$ & 0.81 & 0.913943 & 0.818038 & 0.895721 \\
\bottomrule
\end{tabular}
\end{table}

At join 6 the diamond's contribution recombines with the non-influencing parent by Lemma~\ref{lem:pp-recombination}: parent 13 delivers its signal with probability $0.6561 \times 0.9 = 0.590490$ independently of the group, so the join receives a signal with probability $1 - (1 - 0.720108)(1 - 0.590490) = 0.885382$ and $b(6) = 0.9 \times 0.885382 = 0.796843$. The other three joins carry a single group each, so their beliefs follow directly from the table, $b(11) = 0.733329$, $b(12) = 0.779068$, and $b(T) = 0.806149$.

The dead states of $D_2$, $D_3$ and $D_4$ show that the conditioning weights the two branches; it does not switch one off. With fork 7 down, join 11 is still reached through node 1's side at probability $0.478297$, with fork 14 down, join 12 is still reached through $S_1$'s side at $0.583288$, and with fork 1 down the target still collects the $S_2$ and $S_3$ routes at $0.818038$. Each of those values is itself the output of a full sub-network propagation in which the fork is a pinned local source, and inside $D_4$'s sub-runs the reconvergences at joins 11 and 12 are resolved by their own nested diamonds under the extended context.

The store is visible in the run, which poses twenty conditioned sub-problems of which sixteen are distinct. The four savings are the inner reconvergence at join 6, which $D_3$'s recursion meets again inside each of its states in the form $D_1$ already solved, because node 14 lies outside that sub-problem's span, so the extended context is invisible to its identity and the store answers all four posings. The nested diamonds inside $D_4$ show the opposite case: node 1 lies inside their spans, its pinned state is part of their identity, and each is stored under both outer states, four entries apiece instead of two. The same subgraph is reused where the context cannot reach it and kept apart where it can, which is the context-aware identity of Chapter~\ref{ch:diamond-module} applied at propagation level. Every belief above agrees with exhaustive path enumeration under inclusion-exclusion to floating-point round-off.

\section{Validation}
\label{sec:pp-validation}

\subsection{The corpus and the oracle}
\label{sec:pp-corpus}

Every guarantee in this chapter was checked against implementations that share none of the
propagation's machinery. The exactness oracle is a reduced ordered binary decision diagram
with dynamic variable reordering, built independently for each network; exhaustive path
enumeration is used where it is tractable and Monte Carlo sampling beyond that. The corpus
comprises 129 random and mutated DAGs (120 random graphs across five sizes from 10 to 25
nodes and three densities, eight mutants of a 28-node base graph, and one hand-built
adversarial counterexample), six structured families, larger random graphs of 30 nodes, and
the named networks of Table~\ref{tab:pp-structured}. The provenance of every named network
is given in Appendix~\ref{app:network-specs}; the corpus graphs are generated from recorded
seeds and can be rebuilt byte for byte.

\begin{table}[htbp]
\centering
\caption{Exactness and structure on four named networks against the sifted decision-diagram
oracle. Maximal and unique are the counts of maximal and unique diamonds; max $|C|$ is the largest
conditioning set; the last column is the worst per-node deviation from the oracle.}
\label{tab:pp-structured}
\footnotesize
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}lrrrrrrr@{}}
\toprule
Network & $|V|$ & $|E|$ & Src & Maximal & Uniq & Max $|C|$ & Deviation \\
\midrule
power-network & 23 & 27 & 3 & 5 & 9 & 3 & $8.3\times10^{-17}$ \\
grid-graph & 16 & 24 & 3 & 8 & 41 & 7 & $1.1\times10^{-16}$ \\
KarlNetwork & 26 & 74 & 1 & 18 & 147 & 11 & $5.6\times10^{-17}$ \\
counterexample-n15 & 15 & 23 & 1 & 4 & 13 & 4 & $5.6\times10^{-17}$ \\
\bottomrule
\end{tabular}
\end{table}

Two regimes were run on every graph: a perfect regime with node priors 1 and link probability 0.9, and an imperfect regime with random priors and link probabilities. In both, the propagated point beliefs agree with the oracle to a worst per-node deviation of $1.1 \times 10^{-16}$, and the propagated intervals coincide with the oracle's corner evaluations to at worst $2.8 \times 10^{-16}$. The corpus was run twice: once during the development of the method
and once after the correction of the eligibility test described in
Chapter~\ref{ch:diamond-module}. The second run reproduced all 129 results to within
$10^{-9}$, which is expected, since the corpus draws every prior from the open interval
$(0.3, 0.99)$ and so never contains the prior of exactly one on a non-source node that the
original test mishandled. The correction changed nothing on this corpus and was found
instead on the real networks of Section~\ref{sec:pp-case}, where hub priors of exactly one
occur.

Naive topological ordering of the decision diagram exceeds $2.5 \times 10^{6}$ nodes on the
densest corpus graphs, whereas sifting keeps every graph in the corpus to at most 20,323
nodes. Every comparison below is against the sifted diagram.

\subsection{Two published benchmarks}
\label{sec:pp-repro}

Two networks in the corpus come with published exact reliabilities, and both are reproduced.
The first is the sixteen-node directed grid of Tong and Tien \cite{tong2019directed}, sources
1, 3 and 13, node priors 1, under link reliabilities 0.9 and 0.1. The source reports exact
sink reliabilities to five decimal places at thirteen sinks for each regime. The toolkit's
beliefs agree with every one of the twenty-six published values to within
$5 \times 10^{-6}$, which is the rounding of a five-decimal table; the largest differences,
$4.8 \times 10^{-6}$ at node 12 under 0.9 and $4.3 \times 10^{-6}$ at node 16 under 0.1, are
both below half a unit in the last published digit. The source's own approximate method
reports a gap of $4.8 \times 10^{-4}$ at node 16 under 0.9 between its lower and upper
bounds; the toolkit's value is exact and lies within that gap.

The second is the 23-node directed power distribution network of the same source, Figure 11
there, with sources 1, 7 and 18 and sink 23, under a single link reliability $R_l$ on every
link and node priors 1. Table~\ref{tab:pp-power-repro} compares the toolkit's sink belief with
the published exact value at the three regimes reported. At $R_l = 0.99$ the two agree to all
five published digits. At $R_l = 0.9$ and $0.3$ the toolkit's values are higher by
$1.8 \times 10^{-3}$ and $5.1 \times 10^{-4}$. These residuals are too large to be print
rounding and too small to be a missing or extra path: adding the one edge that the figure
could be read as containing beyond the 27 used here makes every regime worse, by
$2.8 \times 10^{-2}$ at $R_l = 0.9$. The decision-diagram oracle agrees with the toolkit to
$1.1 \times 10^{-16}$ at all three regimes on this topology, so the residual is not in the
computation. It is attributed to a difference in how the network was transcribed from the
figure or in the source's own evaluation, and it is left open. The same network is the
demonstration case of Chapter~\ref{ch:front-end}.

\begin{table}[htbp]
\centering
\caption{Reproduction of the published power-network reliabilities of Tong and Tien
\cite{tong2019directed}, sink 23, node priors 1, every link at $R_l$. The oracle column is
the sifted decision diagram on the same topology.}
\label{tab:pp-power-repro}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lrrrr}
\toprule
$R_l$ & Published & PPA & Oracle & PPA $-$ published \\
\midrule
0.90 & 0.85741 & 0.85917 & 0.85917 & $+0.00176$ \\
0.99 & 0.98969 & 0.98969 & 0.98969 & $0$ \\
0.30 & 0.00221 & 0.00272 & 0.00272 & $+0.00051$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Timing convention}
\label{sec:pp-timing}

Every runtime in this chapter is a wall-clock measurement on one core after a discarded
warm-up run, so that just-in-time compilation is excluded; the convention is stated once in
Appendix~\ref{app:full-results}. The decision-diagram times were measured in the same
process and the same language, so that no cross-language artefact enters the comparison.

\section{Complexity Analysis}
\label{sec:pp-complexity}

The cost of the propagation is concentrated in the diamonds, because everything outside them
is the linear-time update of Eq.~\eqref{eq:pp-independent} applied once per node. A diamond
with conditioning set $C$ and edge list $E_D$ is resolved by $2^{|C|}$ sub-runs, each a single
pass over its edge list, so one resolution costs $2^{|C|} \cdot O(|E_D|)$, and the total work
sums this over every sub-problem the recursion poses. The sum ranges over posings, not over
unique diamonds; nesting re-poses the same diamond under many enclosing states, and on
deeply nested networks the posing count exceeds the unique-diamond sum by orders of
magnitude.

Both refinements of Section~\ref{sec:pp-supernodes} act on this sum. The store collapses the
posings to the distinct sub-problems, which across the corpus is between one and five per
cent of the unmemoised bound, so the posing bound is a sound a-priori budget, computable
from the decomposition before any propagation runs, and the realised cost is far below it.
The factorisation of Lemma~\ref{lem:pp-recombination} acts on the exponents, replacing one
joint enumeration by a sum of small ones. On the adversarial fan-in family, in which $k$
independent reconvergent branches meet at one join, the implementation resolves exactly
$2k+1$ sub-problems for every $k$ tested up to 16 (Table~\ref{tab:pp-adversarial}), where
joint conditioning would resolve $2^k$.

However, no refinement removes the width. The governing parameter is the largest
conditioning set the network forces. The propagation is a specialisation of cutset
conditioning \cite{pearl1988probabilistic} to source-to-node reachability, with two
departures. Where cutset conditioning selects one global set whose enumeration renders the
whole network singly connected, the propagation identifies a local separator at each
reconvergence it meets and recurses, so the realised cost is a sum of local exponentials,
and tree-shaped regions are crossed at linear cost. And where the classical scheme conditions
jointly, the factorisation refuses to enumerate independent forks together.

The measurements place the method. Figure~\ref{fig:pp-width} plots the maximum conditioning
set against the logarithm of the sifted decision diagram's node count across the corpus, and
the two track each other, seven against $\log_2 290 \approx 8.2$ on the sixteen-node grid,
because both measure the same structural width. Realised work tells the same story with no
winner: on some networks the propagation resolves fewer sub-problems than the diagram holds
nodes and on others more, and no family in the corpus systematically favours either method
for exact point-valued computation.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.72\linewidth]{fig04_width_correlation/width_correlation.pdf}
  \caption{Maximum conditioning-set size against the logarithm of the sifted decision
  diagram's node count across the complexity-validation networks. Both quantities measure
  the same structural width.}
  \label{fig:pp-width}
\end{figure}

Two adversarial families locate where the two methods agree and where they part
(Table~\ref{tab:pp-adversarial} and Figure~\ref{fig:pp-crossover}). On fan-in-$k$, $k$
independent gadgets reconverging at one sink, factorisation makes the propagation linear
and the diagram is linear too; the two tie. On mesh-$w$, a width-$w$ lattice with one
correlated block and no independent partition, the propagation's conditioning nests and its
operation count grows far faster than the diagram's node count, which stays compact under
sifting; at $w = 8$ the propagation resolves $3.9 \times 10^{6}$ sub-problems against a
diagram of 2,621 nodes. On square meshes both grow exponentially with the diagram's exponent
the smaller. Hence the propagation is never structurally better than a well-ordered
diagram for exact point-valued reliability: the two tie on independent structure and the
diagram wins on dense correlated structure. The toolkit's case rests on the two capabilities
that follow, which a decision diagram does not offer.

\begin{table}[htbp]
\centering
\caption{Complexity on adversarial families. Operations are the propagation's deduplicated
sub-problem count; nodes are the sifted decision diagram's size. Small instances were
validated exact against path enumeration.}
\label{tab:pp-adversarial}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lrrrrl}
\toprule
Family & Parameter & PPA operations & Max $|C|$ & Diagram nodes & Regime \\
\midrule
fan-in-$k$ & 4  & 9  & 1  & 126 & tie \\
fan-in-$k$ & 8  & 17 & 1  & 474 & tie \\
fan-in-$k$ & 12 & 25 & 1  & 636 & tie \\
fan-in-$k$ & 16 & 33 & 1  & 802 & tie \\
mesh-$w$ & 2 & 78 & 6 & 315 & -- \\
mesh-$w$ & 4 & 20{,}603 & 8 & 700 & diagram \\
mesh-$w$ & 6 & 832{,}922 & 10 & 1{,}487 & diagram \\
mesh-$w$ & 8 & 3{,}895{,}252 & 11 & 2{,}621 & diagram \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\linewidth]{fig07_crossover/fig07_crossover.pdf}
  \caption{Cost on the two adversarial families: the propagation's deduplicated sub-problem
  count and the sifted decision diagram's node count against the family parameter, on a
  logarithmic vertical axis. Left, fan-in-$k$; right, mesh-$w$.}
  \label{fig:pp-crossover}
\end{figure}

On a set of 17 Bayesian-network benchmarks from the bnlearn repository, unrelated to the
infrastructure networks that motivate this thesis, identification completed on every
network in under two seconds and propagation completed in point and interval form on 16 of
the 17. The exception, with a maximum conditioning width of about 21, exhausted memory at
identification; the sifted diagram on the same network exhausted its time budget during
construction. The two methods fail on the same network for the same reason, which is the
width, and the toolkit's applicability extends to probabilistic graphical models generally.

\FloatBarrier
\section{Propagating Uncertain Probabilities}
\label{sec:pp-imprecise}

The inputs of a reliability analysis are rarely known exactly. Failure probabilities come from sparse operational records and expert judgement, and Chapter~\ref{ch:system-model} gave the framework three value forms to carry those states of knowledge: a deterministic value asserting full knowledge, an interval asserting only bounds, and a probability box bounding a distribution. However, in practice these forms are usually collapsed before the analysis begins: a point estimate is chosen and the uncertainty is deferred to a sensitivity study afterwards. The toolkit propagates the stated form itself, because everything the propagation does to a value is one of three operations, complement against one, product, and the weighted combinations of Eq.~\eqref{eq:pp-enumeration}, and none of the machinery of Sections~\ref{sec:pp-conditioning} and~\ref{sec:pp-supernodes} requires the values to be numbers. The framework realises the propagation once over all three forms. What is guaranteed of the output differs by form: the exact range for intervals (Section~\ref{sec:pp-interval}) and sound distributional bounds for probability boxes (Section~\ref{sec:pp-pbox}).

\subsection{Interval inputs are propagated exactly}
\label{sec:pp-interval}

An interval input asserts that a component's reliability lies between two bounds and nothing more, and the correct output for a node is then the exact range of beliefs attainable over all inputs consistent with the stated bounds. The monotonicity of Section~\ref{sec:pp-belief} makes that range available outright.

\begin{proposition}[Corner exactness]
\label{prop:pp-corners}
Let every node prior and edge probability be given as an interval, $\pi_v \in [\underline{\pi}_v, \overline{\pi}_v]$ and $\rho_{uv} \in [\underline{\rho}_{uv}, \overline{\rho}_{uv}]$. The exact range of $b(v)$ over all inputs consistent with the intervals is
\[
\bigl[\, b(v)\ \text{at all lower endpoints},\;\; b(v)\ \text{at all upper endpoints} \,\bigr].
\]
\end{proposition}

\begin{proof}
By Proposition~\ref{prop:pp-monotone}, $b(v)$ is non-decreasing in every input coordinate, so over a box its minimum is attained where every coordinate sits at its lower endpoint and its maximum where every coordinate sits at its upper endpoint.
\end{proof}

No dependency problem arises and no corner search is needed. Neither is typical of interval computations. A general function over an input box takes its extremes at unknown corners among the $2^n$ available, and naive interval arithmetic on it over-approximates whenever a variable occurs more than once. Here monotonicity identifies the two extreme corners, and the propagation is arranged so that the arithmetic between them stays exact. Its complement and product are exact interval operations, and the product form of Eq.~\eqref{eq:pp-independent} presents each signal to the arithmetic exactly once. The conditioning combination is evaluated at the endpoint values of each conditioning belief, which suffices because Eq.~\eqref{eq:pp-enumeration} is multilinear in each of them and a multilinear function attains its extremes over a box at its corners. Every step returns an enclosure of the true range, and across the full validation corpus the propagated interval coincides with the two corner propagations to machine precision, at worst $2.8 \times 10^{-16}$ in either bound over all 129 graphs. On the worked network with every non-source prior and every edge widened to $[0.85, 0.95]$, the propagated belief at the target is $[0.634002, 0.931254]$ and agrees with the two corner runs to the last floating-point digit. Structural certainties are kept degenerate in such a widening, a perfect source stays $[1,1]$, because widening a certainty into an uncertainty changes which nodes can carry conditioning and poses a different and harder problem.

The conditioning machinery matters as much here as for point values. For example, an interval propagation that ignores reconvergent dependence stays sound but over-widens, by up to $0.45$ of the unit range on the corpus, wide enough to turn an informative bound into a vacuous one. Exactness of the range comes from the conditioning, and interval arithmetic alone does not give it.

Carrying intervals costs little. The propagation costs a factor of about $1.2$ over point propagation, a few extra floating-point operations per step. For a one-shot query, producing a network's belief range from scratch, it completed faster than the decision-diagram route to the same range, which builds the diagram once and evaluates its two corners, on every one of eight corpus families tested, by factors between $3.9$ and $99.9$. However, the claim is scoped to the one-shot cost, where the diagram's construction counts against it, and a workload that amortises one diagram across many repeated queries on a fixed network is a different and untested scenario.

\section{Probability-Box Propagation}
\label{sec:pp-pbox}

A probability box is the strongest of the three input forms, a pair of bounding distribution functions carrying partial statistical knowledge of an uncertain reliability \cite{ferson2003constructing}, and propagating it gives the strongest output, bounds on the whole distribution of a node's belief, from which any decision quantity over that distribution can be bounded in turn. The extension raises one new mathematical difficulty at the conditioning step, and this section locates it, resolves it, and states what is proved and what is not.

\subsection{The conditioning combination is not a convolution}
\label{sec:pp-pbox-operator}

The chain and union steps of the propagation raise no new issue for distributional values, because the quantities they combine are independent for the same structural reason that makes the point computation exact, and p-box arithmetic under independence is standard \cite{williamson1990probabilistic}. The difficulty sits at the conditioning combination. Taking the conditioning nodes one at a time, Eq.~\eqref{eq:pp-enumeration} is the convex combination
\[
W \cdot A + (1 - W) \cdot B ,
\]
where $W$ is the conditioning node's contextual belief and $A$, $B$ the conditional results of its two states. All three are now uncertain quantities, and two dependencies bind them. $A$ and $B$ are functions of the same upstream reliabilities, so they are dependent on each other, and the two coefficients are one random variable, not two. Convolving the terms as if independent ignores both bindings, and the consequence is unsoundness: probability mass escapes the unit interval, by as much as $0.34$ on the benchmark grid of Section~\ref{sec:pp-complexity}. A distributional result that assigns mass outside $[0,1]$ bounds nothing.

The framework's operator integrates over the weight's own uncertainty. At each discretisation level of $W$'s bounding functions it scales the two branch distributions by $w$ and $1 - w$ and combines them under an explicit bound on their dependence, mixes the results across levels, and envelopes the mixture built on $W$'s lower bounding function with that built on its upper. Applied one conditioning node at a time, the nested two-way combination reproduces the full mixture over all $2^{|C|}$ states. The output is finally intersected with $[0, 1]$, a projection that discards only values a probability cannot take and so can tighten but never invalidate a bound. The projection is guarded: it raises an error if the excursion ever exceeds floating-point round-off by orders of magnitude, so that a soundness regression cannot hide inside it.


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\linewidth]{fig08_hero_cdf/fig08_hero_cdf.pdf}
  \caption{Cumulative distribution of one sink's belief under uniform component inputs on a
  twelve-node random DAG. The toolkit's band brackets the Monte Carlo distribution; the band
  from a convolution operator is shifted to higher reliability and places mass where the
  true distribution has none.}
  \label{fig:pp-hero}
\end{figure}

\subsection{Two dependence bounds, one proved and one open}
\label{sec:pp-pbox-blends}

The dependence bound between the branches is where provability and tightness separate. Neither is the same as soundness. Sound means the computed pair of bounding functions contains the true distribution, and a sound result is never wrong. Tight means the pair is narrow, and a sound result can still be too wide to use. The choice of bound trades one against the other.

Bounding the branches by the Fr\'echet--Hoeffding inequalities is valid under any joint dependence whatsoever, so the resulting belief bounds inherit soundness from a classical theorem \cite{williamson1990probabilistic} with no proof needed here. The cost is width, and on hard cases the width can approach vacuity. The framework's default operator instead restricts the dependence bound to non-negative dependence, on the ground that $A$ and $B$ are both non-decreasing functions of the shared upstream reliabilities and so cannot move against each other. The restricted operator is tighter and never vacuous in practice, and it has produced no soundness violation in any of the fifty validation configurations checked against Monte Carlo ground truth. But the motivating argument is not a proof. The dependence-bound literature offers a strict trichotomy, independence, a fully specified dependence, or no assumption at all, with no theorem for positive-but-otherwise-unknown dependence, and an attempted derivation through the natural ordering-of-dependence route meets an obstruction that is not a technicality: the comparison function is not uniformly ordered, and independent against comonotone uniform inputs gives combined distributions that cross. Soundness of the restricted operator therefore stands as an open question, stated as such, and the framework keeps the distinction operational: the restricted operator is the default, and the Fr\'echet operator is used wherever a claim must rest on proof alone.

\subsection{What the bounds are worth}
\label{sec:pp-pbox-value}

Soundness never varies across the validation sweep, and tightness does, so the output is characterised by an envelope of tightness values. Figure~\ref{fig:pp-envelope} sweeps input-distribution shapes, uncertainty widths and reconvergence regimes on the benchmark grid. The bound band is near $0.18$ of the unit range under high component reliability and weak reconvergence, and about $0.70$ under deep reconvergence with broadly uncertain components. It is driven by the regime and the input width; the shape of the input distributions has little effect. There are no violations anywhere in the sweep. Deep reconvergence under broad uncertainty is where the branch dependence is strongest and any distribution-free bound on it loosest, so the shape of the envelope follows from the dependence structure and is not an artefact of the implementation.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{fig05_envelope/envelope.pdf}
  \caption{Tightness envelope of the propagated probability-box bound on the benchmark grid: band width against component uncertainty width, across input-distribution shapes and reconvergence regimes.}
  \label{fig:pp-envelope}
\end{figure}

The output is a certificate that no point-valued exact method produces. The output bounds the distribution of $b(v)$, so for a stated reliability requirement $x^{\ast}$ it bounds $P(b(v) \le x^{\ast})$, the probability that the node fails its requirement, analytically and from a single propagation. Table~\ref{tab:pp-certified} shows the certified band on that probability at a representative requirement of $x^{\ast} = 0.95$ on the benchmark grid, between $0.02$ and $0.12$ wide across the regimes tested. Matching that precision by simulation, planned conservatively without foreknowledge of the answer, costs between $267$ and $9{,}604$ samples of the network's uncertain inputs, and the result is a statistical estimate, not a certified bound. The planning figure is the one to quote, because the retrospective sample requirement collapses whenever the true probability sits near $0$ or $1$, correct statistics but knowable only in hindsight. A decision diagram has no analytic route to this quantity and would fall back on simulation.

\begin{table}[htbp]
\centering
\caption{Certified bound on the requirement-violation probability $P(b(v) \le 0.95)$ at the benchmark grid's target node, from one propagation, against the Monte Carlo sample count required to match the certified precision under worst-case a-priori planning.}
\label{tab:pp-certified}
\footnotesize
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}llccc@{}}
\toprule
Regime & Width & Certified band & Band width & MC samples \\
\midrule
perfect components & 0.05 & $[0.00,\, 0.02]$ & 0.02 & 9{,}604 \\
perfect components & 0.10 & $[0.00,\, 0.10]$ & 0.10 & 385 \\
perfect components & 0.15 & $[0.00,\, 0.12]$ & 0.12 & 267 \\
uncertain components & 0.05--0.15 & $[0.98,\, 1.00]$ & 0.02 & 9{,}604 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The cost of the distribution}
\label{sec:pp-pbox-cost}

Distributional propagation is the expensive form. Its cost is set by the discretisation level of the bounding functions and grows faster than linearly with it. Table~\ref{tab:pp-pbox-cost} gives the time at four levels on the fifteen-node reference network and, as an independent measurement on a different network, on the 23-node power network of Section~\ref{sec:pp-repro}. On the reference network the cost rises by factors of about 5, 6.5 and 7.1 per doubling of the level; on the power network by 2.5, 6.75 and 7.8. The two agree at the upper levels and differ at the lowest, where the measurements are a few seconds and sensitive to timing noise. Identification does not depend on the level, so the whole of the cost is in propagation. The bounds are sound at every level, so the level buys precision and not correctness, and levels between $25$ and $100$ are the practical band on networks of the scale the exact method suits. The conditioning depth itself is unchanged across all three value forms, so imprecision never relaxes the structural cost of Section~\ref{sec:pp-complexity}, and a network too reconvergent for exact point propagation is equally beyond exact interval or distributional propagation. Where a network supports exact propagation but not the distributional dial, the exact interval range together with sampling is the practical recourse.


\begin{table}[htbp]
\centering
\caption{Probability-box propagation time by discretisation level, on the fifteen-node
reference network and on the 23-node power network. Identification is under 0.01~s at every
level on both.}
\label{tab:pp-pbox-cost}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lrrrr}
\toprule
Network & Level 25 & Level 50 & Level 100 & Level 200 \\
\midrule
counterexample-n15 & 1.2~s & 6.0~s & 39~s & 276~s \\
power-network & 3.05~s & 7.76~s & 52.4~s & 407~s \\
\bottomrule
\end{tabular}
\end{table}

One consequence of this cost is recorded because it was met in practice. The library the
toolkit uses for probability-box arithmetic sets a default level of 200 when none is
requested. A propagation on the 26-node KarlNetwork of Table~\ref{tab:pp-structured} at
that default ran for seventeen minutes without finishing, while the same propagation at
level 50 completes in 546 seconds; the two figures describe one method at two settings.
Chapter~\ref{ch:front-end} returns to this point, since the interface at present exposes no
control over the level.
\section{Case Study: A Medical Drone Logistics Network}
\label{sec:pp-case}

The three capabilities of this chapter are applied together on a real planning problem. Jones et al.\ designed a conceptual medical drone delivery network for Scotland \cite{jones2025conceptual}, optimising station placement and network structure over real hospital, airport, and candidate-station locations, with two drone types, a short-range vertical take-off type with a nominal range of 70~km and a long-range fixed-wing type with a nominal range of 700~km, trading off delivery time, capital cost, and resilience. Their own resilience analysis works with point probabilities. The question their framework cannot answer, and this toolkit can, is how confidently each location in a candidate design can be called reachable from the supply hubs once the stated uncertainty in the component availabilities is carried through the network.

\subsection{Inputs, traced to their source}
\label{sec:pp-case-inputs}

Every input of the reliability model derives from the source study, with one stated extension. Location availability follows the study's own resilience assumption, hub locations always available and every other location failing independently with probability $0.2$. The hub availability is kept at exactly $1$, and the non-hub availability becomes the interval $[0.75, 0.85]$ centred on the study's figure, because that figure is stated in the study as an assumption, and an interval input makes the whole analysis a sensitivity assessment of that assumption. A connection exists, per drone type, when the inter-location distance is within the type's stated nominal range, the study's own construction rule applied unchanged, and where both types serve a connection the two transmissions combine as independent alternatives.

The one extension concerns weather. The source study holds wind conditions constant in its own analysis and defers weather-driven range variation as an uncertainty that would need expert elicitation. The reliability model extends that deferred point and nothing else. A connection operating comfortably within a drone's range is treated as reliable. A connection whose length falls in the final ten per cent of the range receives the vacuous interval $[0,1]$, the statement that adverse weather may or may not close that route; the ten per cent is an illustrative derating margin, since the study quantifies none. Direction, finally, follows the supply logic. The study's connections are undirected because a route can be flown either way, but the reliability question concerns supply reaching facilities from hubs, so the dependency graph is directed from the hub tier outward, matching the study's own hub-and-spoke architecture.

\subsection{Three designs and the redundancy boundary}
\label{sec:pp-case-designs}

The study's actual optimised layouts are not published, so three configurations stand as proxies for the qualitative character of three of its described trade-off points: a fixed-wing-reliant centralised design serving remote locations through a hub backbone, 217 locations and 263 connections, a densely interconnected short-range design, 242 locations and 1{,}753 connections, and a minimal-investment design retaining only pre-existing infrastructure, 230 locations and 1{,}648 connections. They are proxies for a described character, not reproductions of an unavailable result, and every conclusion below attaches to them as such.

Constructing the dense designs reaches the boundary of exact analysis. Connecting every operationally plausible pair of locations produces a network whose identification alone did not complete, exhausting the memory of the analysis machine within minutes, so unrestricted connectivity sits beyond the practical range of exact analysis outright. The design parameter that controls the boundary is an engineering quantity, the number of alternate routes provisioned per location. It is a redundancy choice made by the designer, and the method has no parameter of its own. Provisioning sixteen routes per location brings the conditioning width of both dense designs to 16, inside the practical ceiling of around eighteen that earlier experience with the method located, and provisioning beyond sixteen changes neither the width nor the runtime materially, so the reconvergent structure of this network saturates there. The centralised design, tree-like by construction, carries width 10. Full exact interval propagation completes in under 25 seconds on the dense designs and in under a second on the centralised one.

The same boundary was measured for the decision-diagram alternative on the same networks, with identical inputs, and the two methods agree to floating-point precision everywhere both complete. At low redundancy both are comfortably fast, and the one-shot interval computation of the propagation is faster, by roughly a factor of fifty on the centralised design and fifteen on the minimal design provisioned at six routes. Raising the provision, the diagram construction completes up to twelve routes per location, at a build of some 123{,}000 nodes in 45 seconds, and did not complete within a documented budget from fourteen routes, under dynamic reordering, while the propagation's runtime stays near five seconds across the same sweep. The claim this supports is narrow. Both methods are governed by the same structural width, and on this one real network family the propagation's practical range, measured along a real redundancy parameter, extends further than the diagram's, with the crossover between twelve and fourteen routes.

Distributional propagation has its own, tighter boundary on the same family, as Section~\ref{sec:pp-pbox-cost} would predict. The full probability-box analysis completes in seconds on the centralised design and in minutes on the minimal design provisioned at six routes, and at eight routes it did not complete within an hour, so distributional propagation is affordable on the sparser designs, and interval propagation, which is exact and cheap, is what scales to the dense ones.

The structural figures of Table~\ref{tab:pp-drone} were remeasured after the correction of
the eligibility test described in Chapter~\ref{ch:diamond-module}, because the drone
designs, unlike the synthetic corpus, contain hub nodes with prior exactly one. The
correction reduced the maximum conditioning width of the dense decentralised design from 17
to 16 and left every belief unchanged, which is the expected signature: the original test
inflated the diamond count around such hubs without affecting the values.

\subsection{Interpreting the propagated bounds}
\label{sec:pp-case-results}

Table~\ref{tab:pp-drone} collects the propagated reliability bounds, and they differ in the way the designs differ. In the centralised design every location's band is narrow, at most $0.10$ wide with a mean of $0.089$: reachability inherits the availability band assumed for the locations, because a tree accumulates little dependence, and it has no protection, since any single-route failure disconnects service. The dense designs spread. Well-connected locations are reachable with near-certainty, and the worst-served facility, Islay Hospital, an island site, carries a reachability probability bounded between $0.56$ and $0.72$ in both dense designs. That interval is the answer to the planning question under the stated input uncertainty, and it is a different kind of answer from the one a point analysis gives. A facility whose reachability is confidently $0.6$ and one whose reachability is anywhere between $0.56$ and $0.72$ call for different interventions: better routes for the first, and better component data before committing capital for the second. No point-valued analysis of the same design can tell them apart. Figure~\ref{fig:pp-dronemap} maps the dense decentralised design coloured by the lower reliability bound and by band width. The uncertainty concentrates at island and far-northern facilities, which is where additional redundancy, or better availability data, buys the most, and the map places the toolkit's output on the geography the source study planned over.

\begin{table}[htbp]
\centering
\caption{The three proxy designs: structure, conditioning width, and propagated interval reliability bounds. Every belief is contained in $[0,1]$ with lower bound at most upper bound. The worst-served facility in both dense designs is Islay Hospital.}
\label{tab:pp-drone}
\footnotesize
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}lrrrccc@{}}
\toprule
Design & $|V|$ & $|E|$ & width & mean band & max band & worst facility \\
\midrule
fixed-wing centralised & 217 & 263 & 10 & 0.089 & 0.100 & $[0.750,\, 0.850]$ \\
dense decentralised & 242 & 1753 & 16 & 0.094 & 0.161 & $[0.562,\, 0.722]$ \\
concentrated minimal & 230 & 1648 & 16 & 0.093 & 0.161 & $[0.562,\, 0.722]$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig06_drone_map/drone_reliability_map.pdf}
  \caption{Reliability map of the dense decentralised design: each facility coloured by the lower bound of its propagated reachability (left, darker is less certainly reachable) and by the width of its reliability band (right, darker is more uncertain). Hubs, drawn as diamonds, are assumed always available. Uncertainty concentrates at island and far-northern facilities, and the worst-served facility, Islay Hospital, is bounded at $[0.56, 0.72]$.}
  \label{fig:pp-dronemap}
\end{figure}

The case study also gives the design process a measured trade-off. More provisioned redundancy improves resilience and simultaneously raises the cost of verifying it exactly, and on this network that trade-off is a measured curve: the width saturates at sixteen routes per location, exact verification is practical there, and the point at which the alternative exact method stops being practical sits measurably earlier. Hence a designer choosing between these configurations can see, in the same analysis, which facilities are uncertain, why they are uncertain, and what verifying a more redundant design will cost.

\section{Discussion}
\label{sec:pp-discussion}

For exact point-valued reliability the toolkit offers no asymptotic advantage over a
well-ordered decision diagram. Both are exponential in a width of the network, the
conditioning width for the toolkit and the pathwidth for the diagram, and across the corpus
neither dominates; the toolkit's operation count tracks the sifted diagram's node count to
within a constant factor. Being exact, the toolkit is intended for the sparse to moderate
regime, and on the corpus it is exact and practical to about fifty nodes, with dense or
deeply reconvergent structure the limiting case, a ceiling shared with every exact method.

For interval inputs the toolkit returns the exact range at machine precision, because
belief is monotone in every input, and it does so at lower one-shot cost than evaluating a
diagram at the two corners. For probability-box inputs it returns sound bounds on the
distribution of every belief. Their tightness is structure-dependent: tight for
high-reliability or weakly reconvergent systems and conservative, though still sound, for
strongly reconvergent systems with broadly uncertain components. Probability-box propagation
is also costly, worse than quadratic in the discretisation level, and in the present
implementation practical only for small networks; for larger networks the recommended
combination is interval propagation together with Monte Carlo sampling. The bounds are sound
at the discretisation level up to floating-point round-off, as is standard for
Williamson--Downs arithmetic.

Two operators are provided for the recombination step. The Fr\'{e}chet operator is sound for
any dependence by the classical theorem. The positive-dependence operator is tighter and is
sound on every configuration tested, but its soundness is not proved as a theorem, and this
is stated as an open problem. The published power-network reproduction of
Section~\ref{sec:pp-repro} carries a residual of order $10^{-3}$ at two of three regimes
that is not a computation error and is not explained; it is also left open.

The toolkit addresses directed acyclic structures. A cyclic network is analysed only after
the orientation of Chapter~\ref{ch:system-model}, and the general cyclic case is outside the
present scope.

\section{Summary}
\label{sec:pp-summary}

The belief of every node, the probability that it operates and is reached from a source,
has been computed in one pass over the layered order, with the closed-form update wherever
parent signals are independent and conditional enumeration on the diamonds of
Chapter~\ref{ch:diamond-module} wherever they are not.
Lemmas~\ref{lem:pp-invariance} to~\ref{lem:pp-supernode} carry the exactness argument, and
the supernode reduction with its store makes the recursion progressive and non-repeating.
The propagation has been validated to machine precision against a decision-diagram oracle on 129 graphs and on named and adversarial families. It reproduces the published exact values of a grid benchmark at every sink, and of a power distribution network at one of three regimes, with a small open residual at the other two. Its cost is governed by the
conditioning width and is competitive with, but not better than, a well-ordered decision
diagram. Extended to intervals, the propagation is exact and cheaper than the diagram's
corner route; extended to probability boxes, it is sound, with a characterised tightness
envelope and a cost that grows steeply with the discretisation level. On the medical drone
network the three capabilities give a designer, in one analysis, the reachability bounds of
every facility, the redundancy setting at which exact verification stops being practical,
and the facilities whose uncertainty is structural.

\section*{Publications}
\addcontentsline{toc}{section}{Publications}

The method of this chapter was first published as the Information Propagation Method at the
2023 International Conference on System Reliability and Safety
\cite{ohiani_information_2023}. The exactness proofs, the interval and probability-box
extensions and the corpus validation are the subject of a paper submitted to
\textit{Reliability Engineering \& System Safety}.

\ifdefined\maindoc
% do nothing
\else
\printbibliography
\end{document}
\fi